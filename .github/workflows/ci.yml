name: CI
on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      # Same target you run locally. The site has no build step, so this
      # validates what is committed rather than what some pipeline produced.
      - run: make check
